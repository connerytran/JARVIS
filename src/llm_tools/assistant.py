
def follow_up(follow_up_question: str):
    """
    This function is called when the assistant determines that a follow-up question is needed to complete the user's request. 
    It should prompt the user for additional information and return that information as a string.
    """
